<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="photostoryTotalList">
	
	<!-- 포토스토리 리스트 -->
	<select id="list" parameterType="ListParameter" resultType="PhotostoryTotalListDto">
		select * from (
			select rownum rn, tmp.* from (
				select * from photostory_total_list
				<where>
					<if test="searchType != null and searchKeyword != null">
						<![CDATA[
							instr(${searchType}, #{searchKeyword}) > 0
						]]>
					</if>
				</where>
				order by photostory_date desc
			) tmp
		) where rn between #{startRow} and #{endRow}
	</select>
	
</mapper>